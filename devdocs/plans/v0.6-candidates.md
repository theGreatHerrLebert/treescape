# v0.6 candidates

Collected while building v0.5. Not a plan: `plan-v0.6.md` picks a tight subset from here, in the usual cadence (conventions → claims → reference → Rust → review).

## Features

### Trees from distance matrices

Today treescape starts from a Newick string, the standard hand-off from inference tools (IQ-TREE, RAxML, MrBayes, BEAST). But many Python users only have a pairwise distance matrix (from an alignment, k-mers, Mash) or a SciPy `linkage` result, and must glue in another library to get a tree.

```python
TreePlot.from_distances(D, labels, method="nj")   # or "upgma"
TreePlot.from_linkage(Z, labels)                  # scipy.cluster.hierarchy
```

Julia: `TreePlot(D, labels; method=:nj)`.

- **Neighbor-Joining** (Saitou & Nei 1987): clock-free, O(n³). **UPGMA**: average linkage, ultrametric.
- Implement in `treescape-core`, reference first in `treescape-reference`.
- **Independent oracles:** ape `nj()` / `upgma` via phangorn (R, release tier), scikit-bio `nj`, Biopython `DistanceTreeConstructor`, SciPy `linkage(method="average")` for UPGMA topology and heights.
- **Convention to pin first:** tie-breaking when several pairs share the minimum Q / distance (the result depends on it; oracles differ), negative NJ branch lengths (keep, clamp, or redistribute — ape keeps them), rooting of the unrooted NJ tree (midpoint? leave unrooted and root at the last join?).
- Input validation: square, symmetric, zero diagonal, finite, labels unique.

### Course use cases (moved from v0.6, 2026-09-21)

The SS26 Bioinformatics Practical demos (students port MATLAB to Julia; the use-case files are listed in the project memory) as real-world fixtures, and a "Coming from MATLAB" page mapping `seqpdist` / `seqneighjoin` / `seqlinkage` / `plot(tree)` to treescape in Python and Julia. Needs the user's OK to publish the course-derived data (matrices, labels, GenBank accessions). No MATLAB is available, so there is no MATLAB oracle; matrices would come from a pinned Biopython alignment + Jukes–Cantor script. Pairs naturally with **distances from sequences** (`seqpdist` equivalents), itself a v0.7 candidate.

### Smaller items

- **Fan layouts from the API.** `start_angle` / `sweep_total` exist in the core but neither `TreePlot` API exposes them.
- **matplotlib colormaps as `cmap=`.** Callables returning 0–1 float RGBA tuples are currently read as 0–255 ints (near-black). Accept float tuples, or detect matplotlib `Colormap` objects.
- **Tested `from_linkage` example** with a gallery image (v0.6 mentions it in one sentence; its behaviour is covered by API and parity tests).
- **Python notebook display.** `TreePlot._repr_svg_` (Julia already has `show(::MIME"image/svg+xml")`).

## Performance

- **Faster neighbor joining (v0.7, decided 2026-09-21).** v0.6's NJ is the exact O(n³) algorithm with the pinned left-to-right row totals, 2.0–3.7× slower than scikit-bio at 500–2000 taxa (UPGMA, with cached row minima, is 2.3–3.9× slower than SciPy). Options, each a convention change or a new claim: maintain row totals incrementally (changes the pinned summation order, so the reference changes with it); RapidNJ-style bounds on Q to skip most of the scan; a nearest-neighbour chain for UPGMA (tie behaviour must be re-pinned). v0.6 already keeps three optimizations that change no result: an active-order compacted matrix and overlapped row totals for NJ, and cached row minima for UPGMA.


- **Sub-quadratic branch styling.** Metadata-driven branch styling is O(nodes × depth) (~0.3 s at 8k tips on a ladder tree). A faster algorithm must preserve the pinned left-to-right Neumaier summation per subtree.

## Distribution

- **JLL + General registry for Treescape.jl.** BinaryBuilder recipe in Yggdrasil for `treescape-jl-connector`, once the C ABI has survived a minor version.
- **PyPI wheels** for `treescape-connector`, `treescape`, `treescape-reference`; the DejaVu license must be included in the connector wheel (see `THIRD_PARTY_NOTICES.md`).

## Evidence

- **Phylo.jl as an independent layout oracle** (Julia lineage), if its coordinates can be extracted from the Plots recipe.
- Items from the v0.5 claims audit that do not land before the v0.5.0 tag are added here when the audit is resolved.
