# treescape

User-facing Python package — the `TreePlot` grammar.

```python
from treescape import TreePlot

TreePlot("tree.nwk").save("tree.svg")
```

See the workspace root README for architecture and the trust manifest.
