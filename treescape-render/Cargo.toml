[package]
name = "treescape-render"
version.workspace = true
edition.workspace = true
license = "MIT AND Bitstream-Vera"  # embeds DejaVu Sans; see THIRD_PARTY_NOTICES.md
repository.workspace = true
description = "Deterministic SVG emitter and themes for treescape scene graphs."

[lib]
name = "treescape_render"

[dependencies]
treescape-core = { path = "../treescape-core", version = "0.6.0" }
fontdue = "0.9"
